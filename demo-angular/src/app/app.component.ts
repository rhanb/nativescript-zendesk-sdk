import { ZendeskSdk } from 'nativescript-zendesk-sdk';
console.log(new ZendeskSdk().message);
import { Component } from "@angular/core";

@Component({
    moduleId: module.id,
    selector: "ns-app",
    templateUrl: "app.component.html"
})
export class AppComponent { }
